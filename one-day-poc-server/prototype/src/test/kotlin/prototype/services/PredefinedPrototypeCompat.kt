package prototype.services

import kotlinx.serialization.json.JsonObject

/**
 * Compatibility class for PredefinedPrototype.
 * This class delegates to the actual PredefinedPrototype class in the prototype package.
 */
typealias PredefinedPrototype = prototype.PredefinedPrototype

/**
 * Compatibility class for PrototypeTemplate.
 * This class delegates to the actual PrototypeTemplate class in the prototype package.
 */
typealias PrototypeTemplate = prototype.PrototypeTemplate