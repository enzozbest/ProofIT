ktor {
    development = true
    deployment {
        port = 8000
    }

    application {
        modules = ["kcl.seg.rtt.ServerKt.configurePlugins", "kcl.seg.rtt.routes.RoutesKt.configureRouting", "kcl.seg.rtt.auth.AuthenticationKt.authModule"]
    }
}
